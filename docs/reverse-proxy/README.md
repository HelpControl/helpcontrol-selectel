# Reverse Proxy

```bash
$npx nx generate @nrwl/express:application reverse-proxy-api --directory=selectel --frontendProject=selectel-mobile --js  --tags=type:api,scope:proxy --no-interactive --dry-run
```
