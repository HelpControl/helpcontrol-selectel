import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile',
  webDir: '../../../dist/apps/selectel/mobile',
  bundledWebRuntime: false,
};

export default config;
